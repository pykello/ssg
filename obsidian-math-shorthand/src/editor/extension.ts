import { Prec, RangeSetBuilder } from "@codemirror/state";
import { Decoration, DecorationSet, EditorView, ViewPlugin, ViewUpdate } from "@codemirror/view";
import { expandMathShorthand } from "../shorthand/expand";
import { mathShorthandEnabled, scanMathRanges } from "../shorthand/scanner";
import { MathWidget } from "./math-widget";

function decorations(view:EditorView,globalEnabled:()=>boolean){const builder=new RangeSetBuilder<Decoration>();const source=view.state.doc.toString();if(!mathShorthandEnabled(source,globalEnabled()))return builder.finish();const selected=view.state.selection.ranges;const visible=view.visibleRanges;for(const range of scanMathRanges(source)){if(!visible.some(viewport=>range.from<=viewport.to&&range.to>=viewport.from))continue;if(selected.some(selection=>selection.from<=range.to&&selection.to>=range.from))continue;const raw=source.slice(range.contentFrom,range.contentTo);const expanded=expandMathShorthand(raw);if(expanded!==raw){const startsLine=view.state.doc.lineAt(range.from).from===range.from;const endsLine=view.state.doc.lineAt(range.to).to===range.to;builder.add(range.from,range.to,Decoration.replace({widget:new MathWidget(expanded,range.display),block:range.display&&startsLine&&endsLine}));}}return builder.finish();}
export function mathShorthandEditorExtension(enabled:()=>boolean){return Prec.high(ViewPlugin.fromClass(class {decorations:DecorationSet;constructor(view:EditorView){this.decorations=decorations(view,enabled);}update(update:ViewUpdate){if(update.docChanged||update.selectionSet||update.viewportChanged)this.decorations=decorations(update.view,enabled);}},{decorations:value=>value.decorations}));}
